# 문항반응이론(IRT) 분석 계산 및 해석 설명서

마지막 업데이트: 2026-05-27

이 문서는 프로그램 안에서 문항반응이론(IRT) 기반 분석이 어떤 데이터로, 어떤 계산 과정을 거쳐, 화면에서 어떻게 해석되는지 설명합니다.

## 1. 최종 검토 요약

현재 프로그램의 IRT 분석 코드는 실행과 계산 흐름 면에서는 정상적으로 작성되어 있습니다. `npm.cmd run build`로 TypeScript 및 Vite 빌드를 확인했으며 오류 없이 통과했습니다.

프로그램 안의 IRT 분석은 두 층으로 나뉩니다.

첫째, `문항분석` 탭과 `학생 상세` 화면의 IRT 값은 기존처럼 정답률과 문항-총점 상관을 이용한 현장형 IRT 근사 분석입니다. 빠르게 문항을 점검하고 학생별 지도 포인트를 찾는 데 적합합니다.

둘째, `전문 IRT` 탭은 선택형 정오 반응을 이용해 1PL Rasch 또는 2PL 모형을 반복 추정하고, 선택 사항인 서답형 IRT 입력파일이 있으면 서답형 부분점수에 대해 다분형 순서형 IRT 분석을 추가로 수행합니다.

전문 IRT 탭도 학교 단위 소표본에서는 탐색적 분석으로 보아야 합니다. 전문 통계 패키지의 대규모 문항은행 보정, 동등화, 3PL 추측도 추정과 같은 고위험 분석을 완전히 대체하는 용도는 아닙니다.

따라서 이 분석은 다음 목적에 적합합니다.

- 문항이 쉬웠는지 어려웠는지 빠르게 확인
- 변별도가 낮거나 음수인 문항을 점검
- 학생별로 기대 반응과 실제 반응이 크게 다른 문항 탐색
- 내용영역별 강점과 보완 영역 파악
- 복습, 적정 도전, 상위 도전 문항 추천

다음 목적에는 단독으로 사용하지 않는 것이 좋습니다.

- 고위험 평가의 최종 판정
- 표본이 매우 작은 시험에서 정밀한 능력 비교
- 문항 은행 보정, 동등화, 장기 추적용 IRT 척도화
- 추측도까지 반영한 3PL 분석

## 2. 관련 코드 위치

IRT 관련 계산은 주로 두 파일에 들어 있습니다.

```text
src/utils/analytics.ts
src/utils/studentIrt.ts
src/utils/formalIrt.ts
src/utils/parseSubjectiveIrt.ts
```

화면 표시는 다음 파일에서 이뤄집니다.

```text
src/pages/QuestionAnalysis.tsx
src/pages/StudentDetail.tsx
src/pages/FormalIrtAnalysis.tsx
```

## 3. 사용 데이터

### 선택형 문항

문항별 IRT 근사치는 선택형 문항만 대상으로 계산됩니다.

사용되는 데이터는 다음과 같습니다.

- 문항 번호
- 정답
- 학생별 선택형 응답
- 문항 배점
- 학생 선택형 점수
- 문항정보표의 내용영역, 성취기준, 난이도 라벨

정답은 문항정보표에 있으면 그 값을 사용하고, 없으면 정오표의 선택형 정답을 사용합니다.

### 단답형/서술형 문항

정오표에 단답형과 서술형 문항별 점수 열이 있는 경우에는 각 문항의 부분 점수를 읽어 문항별 득점률, 난이도 근사치, 변별도 근사치를 따로 계산합니다.

단답형/서술형 문항별 점수 열이 없고 서답형 합계 점수만 있는 경우에는 문항별 분석을 만들 수 없습니다. 이때는 서답형 합계 점수 기준의 섹션 수준 참고 지표만 계산합니다.

따라서 화면의 해석 단위는 데이터 구성에 따라 다음처럼 달라집니다.

- 선택형: 문항별 정오 반응 기준
- 단답형: 문항별 점수 열이 있으면 단답형 문항별 점수 기준
- 서술형: 문항별 점수 열이 있으면 서술형 문항별 점수 기준
- 서답형 통합: 문항별 점수 열이 없으면 서답형 합계 점수 기준

### 전문 IRT 탭의 추가 데이터

전문 IRT 탭은 기본적으로 선택형 정오 반응만으로 작동합니다.

서답형까지 전문 분석하려면 별도의 서답형 IRT 입력파일이 필요합니다.

필수 시트는 다음과 같습니다.

```text
문항_루브릭
학생_문항점수
범주_채점기준
```

`문항_루브릭`은 문항별 최대점, 점수 범주, 내용영역, 성취기준을 제공합니다.

`학생_문항점수`는 학생별 서답형 문항 원점수를 제공합니다.

`범주_채점기준`은 0점, 1점, 2점 같은 각 점수 범주가 루브릭상 어떤 성취 단계를 의미하는지 제공합니다.

주의:

```text
0점과 결측은 구분해야 합니다.
실제 0점은 숫자 0으로 입력합니다.
결측 또는 미응시는 빈칸으로 둡니다.
점수 범주는 낮은 성취에서 높은 성취로 순서화되어야 합니다.
```

## 4. 문항별 난이도 b 계산

선택형 문항마다 정답 여부를 1 또는 0으로 바꿉니다.

```text
정답 = 1
오답 또는 무응답 = 0
```

문항의 정답률을 `p`라고 할 때, 프로그램은 다음 식으로 IRT 난이도 근사치 `b`를 계산합니다.

```text
b = log((1 - p) / p)
```

극단적인 값이 무한대로 튀는 것을 막기 위해 `p`는 0.01 이상, 0.99 이하로 제한됩니다.

해석은 다음과 같습니다.

- `b < 0`: 정답률이 높은 쉬운 문항
- `b = 0` 근처: 정답률이 약 50%인 중간 난이도 문항
- `b > 0`: 정답률이 낮은 어려운 문항

예를 들면 다음과 같습니다.

```text
정답률 90% -> b = log(0.1 / 0.9) ≈ -2.20
정답률 50% -> b = log(0.5 / 0.5) = 0
정답률 20% -> b = log(0.8 / 0.2) ≈ 1.39
```

주의할 점은, 이 `b`는 정식 IRT 보정으로 얻은 문항 난이도가 아니라 정답률을 logit 척도로 바꾼 근사치라는 점입니다.

## 5. 문항별 변별도 a 계산

문항 변별도 `a`는 해당 문항의 반응 또는 점수와, 같은 유형 안에서 해당 문항을 제외한 나머지 점수 사이의 피어슨 상관으로 계산합니다.

선택형 문항은 해당 문항의 정답 여부와 선택형 내부 나머지 점수를 비교합니다. 서답형 점수는 선택형 문항 변별도 계산에 포함하지 않습니다.

```text
선택형 문항 반응 = 해당 문항 정답 여부(1 또는 0)
선택형 비교 점수 = 학생 선택형 점수 - 해당 문항에서 얻은 선택형 점수
선택형 a 근사치 = pearson(선택형 문항 반응, 선택형 비교 점수)
```

단답형과 서술형은 문항별 점수 열이 있을 때만 문항별 변별도를 계산합니다. 이때도 각 유형 내부 나머지 점수를 비교 점수로 사용합니다.

```text
단답형 문항 점수 = 해당 단답형 문항에서 얻은 점수
단답형 비교 점수 = 학생 단답형 점수 - 해당 단답형 문항 점수
단답형 a 근사치 = pearson(단답형 문항 점수, 단답형 비교 점수)

서술형 문항 점수 = 해당 서술형 문항에서 얻은 점수
서술형 비교 점수 = 학생 서술형 점수 - 해당 서술형 문항 점수
서술형 a 근사치 = pearson(서술형 문항 점수, 서술형 비교 점수)
```

이 방식은 고전검사이론의 수정된 문항-총점 상관에 가깝습니다. 선택형은 정오 반응 기반 point-biserial 성격이고, 단답형/서술형은 부분 점수 기반 문항-나머지 점수 상관입니다.

해석은 다음과 같습니다.

- `a`가 높음: 높은 점수의 학생과 낮은 점수의 학생을 잘 구분한 문항
- `a`가 0에 가까움: 학생 수준과 정답 여부의 관련이 약한 문항
- `a`가 음수: 점수가 높은 학생보다 낮은 학생이 더 많이 맞힌 문항일 수 있어 점검 필요

음수 변별도는 정답 오류, 복수정답 처리 오류, 문항 표현 문제, 수업 내용과의 불일치, 우연 반응 등 다양한 원인이 있을 수 있습니다.

## 6. 문항분석 탭에서의 IRT 해석

문항분석 탭에서는 먼저 유형별 IRT 요약 카드가 표시됩니다.

```text
선택형 IRT 요약
단답형 IRT 요약
서술형 IRT 요약
```

단답형/서술형 요약은 해당 문항별 점수 열이 있을 때 표시되는 문항별 지표의 평균입니다. 문항별 점수 열이 없으면 서답형 통합 섹션 점수 기준 참고 지표로 표시됩니다.

선택형 문항별 표에서는 다음 값이 표시됩니다.

```text
IRT a = 문항 변별도 근사치
IRT b = 문항 난이도 근사치
```

또한 선택형 전체 요약은 선택형 문항별 `a`, `b`의 평균으로 계산됩니다.

```text
선택형 평균 a = 선택형 문항별 a의 평균
선택형 평균 b = 선택형 문항별 b의 평균
```

단답형/서술형 문항별 점수 열이 감지되면 별도 표에 다음 값이 표시됩니다.

```text
평균득점 = 해당 문항의 학생 평균 점수
득점률 = 평균득점 / 문항 배점
IRT a = 유형 내부 나머지 점수와의 변별도 근사치
IRT b = 득점률을 logit 척도로 바꾼 난이도 근사치
```

해석할 때는 다음 순서를 권장합니다.

1. 정답률을 먼저 봅니다.
2. `b`로 난이도 분포를 확인합니다.
3. `a`가 낮거나 음수인 문항을 점검합니다.
4. 오답 분포와 성취기준을 함께 확인합니다.
5. 단순히 숫자만 보지 말고 실제 문항 내용을 다시 확인합니다.

## 7. 학생별 theta 계산

학생 상세 화면의 IRT 기반 정밀 분석에서는 선택형 문항 반응을 이용해 학생별 능력 추정치 `theta`를 계산합니다.

먼저 문항분석에서 구한 문항 파라미터를 학생 분석용으로 정리합니다.

```text
문항 난이도 b: -3 이상 3 이하로 제한
문항 변별도 a: 0.15 이상 1.5 이하로 제한
```

현재 코드에서는 음수 변별도도 절댓값으로 바꿔 학생 theta 계산에 사용합니다. 이 덕분에 계산은 안정적으로 진행되지만, 음수 변별도 문항까지 정상 문항처럼 theta에 반영될 수 있습니다. 정밀한 분석을 원한다면 음수 변별도 문항은 학생 theta 계산에서 제외하거나 별도 경고 대상으로만 쓰는 개선이 좋습니다.

학생의 초기 theta는 선택형 정답 개수로부터 계산합니다.

```text
p0 = (정답 개수 + 0.5) / (문항 수 + 1)
초기 theta = log(p0 / (1 - p0))
```

그 다음 2PL 형태의 로지스틱 모형을 사용합니다.

```text
P_i(theta) = 1 / (1 + exp(-a_i * (theta - b_i)))
```

여기서 각 기호의 의미는 다음과 같습니다.

- `P_i(theta)`: 학생이 i번 문항을 맞힐 기대 확률
- `a_i`: i번 문항의 변별도
- `b_i`: i번 문항의 난이도
- `theta`: 학생의 능력 추정치

theta는 뉴턴-랩슨 방식으로 반복 계산합니다.

```text
score = sum(a_i * (u_i - P_i))
info = sum(a_i^2 * P_i * (1 - P_i))
theta = theta + score / info
```

여기서 `u_i`는 학생의 실제 반응입니다.

```text
정답이면 u_i = 1
오답 또는 무응답이면 u_i = 0
```

프로그램은 최대 24회 반복하고, 변화량이 충분히 작아지면 멈춥니다. theta는 -4 이상 4 이하로 제한됩니다.

## 8. theta 표준오차와 백분위

theta 계산이 끝나면 최종 정보량으로 표준오차를 계산합니다.

```text
SE = 1 / sqrt(finalInfo)
```

표준오차가 작을수록 현재 문항 반응으로 theta가 비교적 안정적으로 추정되었다고 볼 수 있습니다.

능력 백분위는 같은 시험을 본 학생들의 theta와 비교해 계산합니다.

```text
백분위 = 자기보다 theta가 낮은 학생 수와 동점 학생 절반을 반영한 비율
```

이 백분위는 전국 규준이 아니라 현재 불러온 시험 데이터 안에서의 상대적 위치입니다.

## 9. 예상 점수와 실제 점수

학생별 theta가 계산되면 각 문항에서 기대 정답 확률을 다시 계산합니다.

```text
기대 정답 수 = sum(P_i(theta))
기대 선택형 점수 = sum(문항 배점 * P_i(theta))
실제 선택형 점수 = sum(문항 배점 * 실제 정답 여부)
```

화면의 "예상 대비 실제"는 이 값을 비교합니다.

해석은 다음과 같습니다.

- 실제 점수 > 기대 점수: 같은 theta에서 기대한 것보다 더 잘 수행
- 실제 점수 < 기대 점수: 실수, 특정 영역 취약, 문항 조건 부적응 가능성
- 차이가 작음: theta와 실제 반응 패턴이 비교적 잘 맞음

## 10. 예상 밖 반응

프로그램은 다음과 같은 반응을 예상 밖 반응으로 표시합니다.

```text
기대 정답 확률이 70% 이상인데 틀린 문항
기대 정답 확률이 30% 이하인데 맞힌 문항
```

표시할 문항이 충분하지 않으면, 실제 반응과 기대 확률의 차이가 큰 문항도 함께 보여줍니다.

해석 예시는 다음과 같습니다.

- 쉬운 편으로 예측되었는데 틀림: 실수, 계산 오류, 개념 공백 가능성
- 어려운 편으로 예측되었는데 맞힘: 강점 영역, 풀이 전략, 우연 정답 가능성

이 목록은 학생 지도에서 "왜 이 반응이 나왔는가"를 확인하기 위한 출발점입니다.

## 11. 내용영역별 강점과 약점

내용영역별로 실제 정답률과 기대 정답률을 비교합니다.

```text
관측 정답률 = 해당 영역 실제 정답 수 / 해당 영역 문항 수
기대 정답률 = 해당 영역 기대 정답 확률 합 / 해당 영역 문항 수
격차 = 관측 정답률 - 기대 정답률
```

해석은 다음과 같습니다.

- 격차가 양수: 같은 theta 기준 기대보다 잘한 영역
- 격차가 음수: 같은 theta 기준 기대보다 못한 영역

즉 단순 정답률이 낮다고 무조건 약점으로 보는 것이 아니라, 학생의 전체 능력 추정치에 비해 더 낮게 나온 영역을 찾습니다.

## 12. 반응 일관성

반응 일관성은 다음 값을 조합해 계산합니다.

- 기대 정답 확률과 실제 반응의 평균 절대 차이
- 기대 정답 확률과 실제 반응의 상관
- 기대와 크게 다른 문항 수

점수는 0점에서 100점 사이로 표시됩니다.

해석은 다음과 같습니다.

- 높음: 문항 난이도 흐름과 학생 반응이 비교적 자연스럽게 맞음
- 낮음: 쉬운 문항 실수, 어려운 문항 우연 정답, 읽기 오류, 컨디션 문제 등을 점검할 필요

이 값은 학생을 평가하는 점수라기보다 반응 패턴을 점검하는 보조 지표입니다.

## 13. 적응형 학습 제안

학생 theta와 문항 난이도 `b`를 비교해 복습 문항을 분류합니다.

```text
기초 보강: 틀린 문항 중 b <= theta - 0.3
적정 도전: 틀린 문항 중 theta 근처의 문항
상위 도전: b >= theta + 0.5인 문항
```

해석은 다음과 같습니다.

- 기초 보강: 현재 수준보다 쉬운 편인데 틀린 문항
- 적정 도전: 현재 수준과 가장 가까운 난이도의 문항
- 상위 도전: 현재 수준보다 어려운 문항

상위 도전 문항은 반드시 틀린 문항만을 의미하지 않습니다. 현재 시험지 안에서 다음 단계 도전용으로 볼 수 있는 문항입니다.

## 14. 전문 IRT 탭의 전체 구조

전문 IRT 탭은 기존 문항분석 화면보다 더 엄밀한 방식으로 문항과 학생을 함께 추정하기 위한 화면입니다.

기본 구성은 다음과 같습니다.

```text
선택형 전문 IRT 분석
데이터 적합성 점검
능력 추정 분포
검사 정보함수
선택형 문항 모수 및 적합도
선택형 상위/보완 우선 학생
```

서답형 IRT 입력파일이 있으면 다음 섹션이 추가됩니다.

```text
서답형 다분형 IRT
서답형 데이터 점검
서답형 θ 분포
서답형 검사 정보함수
서답형 문항 모수 및 범주 임계값
서답형 상위/보완 우선 학생
```

따라서 파일 입력 상태에 따라 전문 IRT 탭의 분석 범위는 다음처럼 달라집니다.

```text
문항정보표 + 정오표만 있음
-> 선택형 전문 IRT만 분석

문항정보표 + 정오표 + 서답형 IRT 입력파일 있음
-> 선택형 전문 IRT와 서답형 다분형 IRT를 함께 분석
```

## 15. 선택형 전문 IRT 분석

선택형 전문 IRT는 학생별 정오 반응을 이용합니다.

각 문항은 다음처럼 처리됩니다.

```text
정답 = 1
오답 = 0
무응답 = 0
```

전문 IRT 탭에서는 표본 수와 문항 수에 따라 모형을 자동으로 선택합니다.

```text
응시자 100명 이상, 보정 가능 문항 10개 이상
-> 2PL 모형

그보다 작음
-> 1PL Rasch 모형
```

1PL Rasch 모형은 모든 문항의 변별도를 같다고 보고 난이도 중심으로 추정합니다. 표본이 작을 때 더 안정적입니다.

2PL 모형은 문항마다 변별도 `a`와 난이도 `b`를 따로 추정합니다. 더 유연하지만 표본이 작으면 모수가 흔들릴 수 있습니다.

전문 IRT 탭에서 제외되는 문항은 다음과 같습니다.

```text
정답키가 없는 문항
전원 정답 문항
전원 오답 문항
응시자 데이터가 없는 문항
```

전원 정답 또는 전원 오답 문항은 정답률 자체는 의미가 있지만, 학생 능력 차이를 이용한 모수 추정에는 정보가 부족하므로 전문 IRT 보정에서 제외됩니다.

## 16. 선택형 전문 IRT 지표 해석

### θ 학생 능력 추정치

`θ`는 학생의 선택형 응답 패턴으로 추정한 상대 능력입니다.

```text
θ = 0 근처: 현재 집단의 평균적 위치
θ > 0: 현재 집단 평균보다 높은 위치
θ < 0: 현재 집단 평균보다 낮은 위치
```

이 값은 전국 규준이 아니라 현재 불러온 시험 데이터 안에서의 상대 척도입니다.

### SE 표준오차

`SE`는 θ 추정의 불확실성입니다.

```text
SE가 작음: 현재 문항들로 학생 위치가 비교적 안정적으로 추정됨
SE가 큼: 문항 수, 문항 난이도, 반응 패턴 때문에 추정이 불안정함
```

SE가 큰 학생은 θ 순위만으로 단정하지 말고 실제 문항 반응과 오답 내용을 함께 봐야 합니다.

### a 변별도

2PL 모형에서 `a`는 문항이 학생 능력 차이를 얼마나 잘 구분하는지 나타냅니다.

```text
a가 높음: 상위/하위 학생을 잘 구분
a가 낮음: 능력 차이에 따른 반응 차이가 약함
```

낮은 변별도 문항은 다음을 점검합니다.

```text
정답키 오류
복수정답 처리 문제
문항 표현의 모호성
수업 내용과 평가 내용의 불일치
너무 쉬운 문항 또는 너무 어려운 문항
```

### b 난이도

`b`는 문항 난이도입니다.

```text
b < 0: 평균보다 쉬운 문항
b = 0 근처: 평균 수준의 문항
b > 0: 평균보다 어려운 문항
```

`b`가 높은 문항은 높은 θ 학생에게 더 많은 정보를 줍니다. `b`가 낮은 문항은 기초 수준 학생을 구분하는 데 더 유용합니다.

### infit / outfit

`infit`과 `outfit`은 문항 반응이 모형 예측과 얼마나 잘 맞는지 보는 적합도 지표입니다.

대략적인 해석은 다음과 같습니다.

```text
0.7 ~ 1.3: 대체로 양호
1.3 ~ 1.5: 점검 권장
1.5 초과: 부적합 가능성 큼
0.5 미만: 반응이 지나치게 예측 가능하거나 중복 정보 가능성
```

`outfit`은 예상 밖의 극단 반응에 더 민감하고, `infit`은 학생 수준 근처의 문항 반응에 더 민감합니다.

적합도가 나쁘다고 해서 문항이 반드시 틀렸다는 뜻은 아닙니다. 실제 문항 내용, 오답 분포, 성취기준을 함께 확인해야 합니다.

### 검사 정보함수

검사 정보함수는 현재 시험지가 어느 θ 구간의 학생을 가장 정밀하게 구분하는지 보여줍니다.

```text
정보량 높음: 해당 능력 구간에서 추정이 정밀함
정보량 낮음: 해당 능력 구간에서 추정이 불안정함
```

예를 들어 정보함수가 θ = 0 근처에서 높으면 평균 수준 학생 구분에 강한 시험입니다. θ = 1 이상에서 높으면 상위권 구분에 더 강한 시험입니다.

### 분리 신뢰도

분리 신뢰도는 학생 θ가 어느 정도 안정적으로 분리되는지 보는 보조 지표입니다.

```text
0.8 이상: 높음
0.7 이상: 양호
0.6 이상: 주의
0.6 미만: 낮음
```

문항 수가 적거나 모든 학생 점수가 비슷하면 신뢰도가 낮아질 수 있습니다.

## 17. 서답형 다분형 IRT 분석

서답형 IRT 입력파일이 있으면 프로그램은 서답형 문항의 부분점수를 순서형 범주로 보고 다분형 IRT 분석을 수행합니다.

현재 구현은 다음 가정을 둡니다.

```text
0점 < 1점 < 2점 < ... < 최대점
각 점수 범주는 낮은 성취에서 높은 성취로 올라가는 단계
학생의 서답형 θ가 높을수록 높은 점수 범주에 도달할 가능성이 커짐
```

따라서 서답형 점수 범주가 실제로 순서형이어야 합니다.

좋은 예:

```text
0점: 풀이 없음 또는 핵심 개념 오류
1점: 개념 단서 일부 제시
2점: 풀이 전략은 맞으나 계산 오류
3점: 거의 완성
4점: 완전 정답
```

주의가 필요한 예:

```text
1점: 계산 방식 A 사용
2점: 계산 방식 B 사용
```

이처럼 점수 범주가 성취 수준의 순서가 아니라 서로 다른 풀이 유형을 뜻한다면, 다분형 IRT 해석이 부적절할 수 있습니다.

## 18. 서답형 지표 해석

### 서답형 θ

서답형 θ는 학생이 서답형 루브릭의 높은 점수 범주에 도달하는 경향을 나타냅니다.

선택형 θ와 반드시 같을 필요는 없습니다.

```text
선택형 θ 높음, 서답형 θ 낮음
-> 개념 선택은 가능하지만 풀이 과정 서술, 논리 전개, 계산 완성도가 약할 수 있음

선택형 θ 낮음, 서답형 θ 높음
-> 객관식 반응보다 과정형 수행에서 강점이 있을 수 있음
```

### 평균득점률

평균득점률은 해당 서답형 문항의 평균 점수를 최대점으로 나눈 값입니다.

```text
평균득점률 = 평균득점 / 최대점
```

이 값은 고전적인 난이도 지표로 먼저 확인합니다.

### location 문항 위치

`location`은 문항의 평균 임계값입니다.

```text
location 낮음: 낮은 θ 학생도 점수를 얻기 쉬운 문항
location 높음: 높은 θ 학생에게도 높은 점수 범주 도달이 어려운 문항
```

선택형의 `b`와 비슷하게 서답형 문항의 전반적 위치를 보는 지표입니다.

### τ 범주 임계값

`τ1`, `τ2`, `τ3` 같은 임계값은 한 단계 높은 점수 범주에 도달하는 기준점입니다.

예를 들어 4점 문항은 보통 다음 임계값을 가집니다.

```text
τ1: 0점에서 1점 이상으로 넘어가는 기준
τ2: 1점에서 2점 이상으로 넘어가는 기준
τ3: 2점에서 3점 이상으로 넘어가는 기준
τ4: 3점에서 4점으로 넘어가는 기준
```

임계값이 높을수록 그 단계로 올라가기 어렵습니다.

임계값이 자연스럽게 증가하지 않거나, 특정 임계값이 과도하게 낮거나 높으면 루브릭 단계가 잘 구분되지 않을 가능성이 있습니다.

### 서답형 infit / outfit

서답형 문항의 infit/outfit도 모형 예측과 실제 부분점수의 차이를 봅니다.

```text
1 근처: 모형과 실제 점수 패턴이 잘 맞음
1.3 초과: 루브릭 또는 채점 일관성 점검 권장
1.5 초과: 부적합 가능성 큼
0.5 미만: 정보가 지나치게 중복되거나 점수 분포가 너무 예측 가능함
```

부적합 가능성이 있는 서답형 문항은 다음을 확인합니다.

```text
부분점수 기준이 실제로 순서형인지
채점자 간 기준이 일관적인지
특정 단계 점수가 거의 사용되지 않았는지
동일 점수 안에 서로 다른 풀이 유형이 섞여 있는지
결측과 0점이 혼동되지 않았는지
```

## 19. 선택형 θ와 서답형 θ를 함께 보는 방법

전문 IRT 탭에서 선택형과 서답형을 모두 분석할 수 있으면, 두 θ를 비교해 학생의 수행 양상을 더 입체적으로 볼 수 있습니다.

해석 예시는 다음과 같습니다.

```text
선택형 θ 높음 + 서답형 θ 높음
-> 개념 이해와 과정 서술이 모두 안정적

선택형 θ 높음 + 서답형 θ 낮음
-> 문제 인식은 좋지만 풀이 과정, 표현, 계산 완성도 보완 필요

선택형 θ 낮음 + 서답형 θ 높음
-> 객관식 반응보다 과정형 설명에서 강점이 나타남

선택형 θ 낮음 + 서답형 θ 낮음
-> 기초 개념과 풀이 과정 모두 단계적 보완 필요
```

두 θ가 크게 다를 때는 반드시 실제 문항과 학생 풀이를 확인해야 합니다. 선택형과 서답형은 측정하는 행동이 다르기 때문에 차이가 나는 것 자체가 오류는 아닙니다.

## 20. 전문 IRT 탭 사용 권장 순서

전문 IRT 탭은 다음 순서로 보는 것이 좋습니다.

1. `데이터 적합성 점검`을 먼저 확인합니다.
2. 표본 수, 문항 수, 제외 문항, 결측 비율 경고를 확인합니다.
3. 검사 정보함수로 시험지가 어느 능력 구간을 잘 측정하는지 봅니다.
4. 문항 모수 표에서 낮은 변별도, 높은 outfit, 임계값 순서 점검 문항을 찾습니다.
5. 학생 θ 표는 SE와 함께 봅니다.
6. 서답형 분석이 있으면 루브릭 단계와 임계값을 함께 해석합니다.
7. 최종 판단은 정답률, 오답 분포, 실제 문항, 학생 풀이, 수업 관찰과 종합합니다.

특히 다음 상황에서는 θ 순위를 강하게 해석하지 않는 것이 좋습니다.

```text
응시자 수가 50명 미만
보정 가능한 문항 수가 8개 미만
서답형 문항 수가 3개 미만
결측 비율이 높음
전원 정답/전원 오답 문항이 많음
분리 신뢰도가 0.6 미만
```

## 21. 현재 코드의 장점

현재 구현은 학교 현장용 시험 분석 도구로 다음 장점이 있습니다.

- 계산 구조가 단순해 데이터가 적어도 빠르게 분석 가능
- 정답률, 변별도, 오답 분포와 함께 해석하도록 구성됨
- 학생별 theta, 기대 점수, 예상 밖 반응, 영역별 격차가 한 흐름으로 연결됨
- 극단값을 제한해 화면에서 무한대나 과도한 값이 나오는 것을 방지함
- 선택형과 서답형의 데이터 한계를 구분해 표시함
- 전문 IRT 탭에서 선택형 1PL/2PL과 서답형 다분형 IRT를 데이터 유무에 따라 분리해 표시함

## 22. 검토 결과 주의할 점

### 1. 정식 IRT로 과대 해석하면 안 됨

문항분석 탭과 학생 상세 화면의 IRT 파라미터는 별도의 보정 절차로 추정한 값이 아니라 정답률과 상관계수에서 얻은 근사값입니다. 따라서 해당 화면에서는 "IRT 기반 보조 분석", "IRT 근사치"라고 표현하는 것이 적절합니다.

전문 IRT 탭은 별도의 반복 추정을 수행하지만, 표본이 작은 학교 단위 데이터에서는 여전히 탐색적 분석으로 해석해야 합니다.

### 2. 음수 변별도 문항 처리

문항분석 탭에서는 음수 변별도를 그대로 표시해 문항 점검이 가능하지만, 학생 theta 계산에서는 절댓값으로 바꿔 사용합니다.

이 방식은 계산 안정성에는 도움이 되지만, 실제로는 문제가 있을 수 있는 문항을 정상 변별 문항처럼 반영하는 효과가 있습니다. 더 엄밀하게 하려면 다음 중 하나가 좋습니다.

- 음수 변별도 문항은 theta 계산에서 제외
- 변별도 0.1 미만 문항은 theta 계산에서 제외
- 문항분석 탭에 "학생 IRT 계산 제외" 표시 추가

### 3. 서답형 문항별 점수 데이터 한계

선택형 문항 변별도는 선택형 내부 점수만 사용하도록 수정되어 있습니다.

단답형/서술형은 정오표에 문항별 점수 열이 있는 경우에만 문항별 변별도를 계산합니다. 문항별 점수 열이 없고 서답형 합계만 있는 경우에는 개별 문항의 변별도를 복원할 수 없으므로, 서답형 합계 점수 기준의 섹션 참고 지표만 표시합니다.

### 4. 극단 문항 처리

모두 맞힌 문항이나 모두 틀린 문항은 변별도 계산이 불가능할 수 있습니다. 현재 학생 theta 분석에서는 이런 문항도 기본 변별도를 부여해 계산에 포함될 수 있습니다.

표본이 작거나 극단 문항이 많으면 theta와 표준오차 해석에 주의해야 합니다.

### 5. 화면의 b와 학생 분석의 b 제한 범위

문항분석 탭의 `b`는 정답률 기반 값을 그대로 보여주지만, 학생 theta 계산에서는 `b`를 -3에서 3 사이로 제한합니다. 정답률이 0% 또는 100%에 가까운 문항은 화면 표시값과 학생 분석 내부값이 약간 다를 수 있습니다.

### 6. 서답형 다분형 IRT 해석 주의

서답형 다분형 IRT는 점수 범주가 순서형이라는 가정을 둡니다. 0점, 1점, 2점이 단순히 다른 오류 유형을 뜻하고 성취 수준의 순서가 아니라면 임계값 해석이 왜곡될 수 있습니다.

또한 서답형 문항 수가 적으면 학생별 θ보다 문항별 평균득점률, 임계값, 적합도, 루브릭 점검을 우선하는 것이 좋습니다.

## 23. 권장 해석 문구

학생 상담이나 교사용 해석에서는 다음처럼 표현하는 것이 안전합니다.

```text
이 분석은 선택형 응답 패턴을 바탕으로 한 IRT 근사 분석입니다.
학생의 전체 능력, 문항 난이도, 실제 반응의 차이를 함께 보아
강점 영역과 보완 영역을 찾기 위한 참고 자료로 활용합니다.
최종 판단은 문항 내용, 풀이 과정, 서답형 수행, 수업 관찰과 함께 종합합니다.
```

전문 IRT 탭을 사용할 때는 다음 문구도 함께 사용할 수 있습니다.

```text
전문 IRT 탭의 θ와 문항 모수는 현재 불러온 시험 자료 안에서
학생 반응과 문항 특성을 함께 추정한 탐색적 지표입니다.
표본 수, 문항 수, 결측, 적합도 경고를 함께 확인하고,
최종 판단은 실제 문항과 학생 풀이를 검토한 뒤 내립니다.
```

## 24. 결론

현재 코드는 프로그램의 목적에 맞는 "현장형 IRT 근사 분석"으로는 대체로 타당하게 구성되어 있습니다. 특히 난이도, 변별도, theta, 예상 밖 반응, 내용영역 격차가 하나의 흐름으로 연결되어 있어 교사가 학생별 지도 포인트를 찾는 데 유용합니다.

전문 IRT 탭이 추가되면서 선택형은 1PL/2PL 모형 기반 분석을, 서답형 IRT 입력파일이 있을 때는 부분점수 기반 다분형 IRT 분석을 함께 볼 수 있습니다.

다만 정식 IRT 분석이라고 단정하기에는 여전히 데이터 조건의 한계가 있습니다. 특히 표본 수가 작거나, 문항 수가 적거나, 서답형 루브릭 단계가 명확하지 않으면 분석 결과를 참고 지표로 사용해야 합니다. 낮은 변별도 문항, 극단 문항, 적합도 경고 문항은 반드시 문항 내용과 채점 기준 검토와 함께 해석하는 것이 가장 안전합니다.
