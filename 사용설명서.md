# 성적 분석 시스템 사용설명서

마지막 업데이트: 2026-05-28

이 프로그램은 엑셀 파일을 불러와 시험 결과를 분석하고, 학생별 관찰 기록과 AI 세특 초안을 생성하는 Windows용 Electron 앱입니다.

## 1. 실행 방법

### 배포용 실행 파일로 실행

실행 파일 위치:

```text
release\ExamAnalysis-win32-x64\ExamAnalysis.exe
```

다른 컴퓨터로 옮길 때는 `ExamAnalysis.exe`만 복사하지 말고 아래 폴더 전체를 압축해서 옮기세요.

```text
release\ExamAnalysis-win32-x64
```

이 폴더 안의 `resources`, `locales`, `.dll` 파일들이 같이 있어야 실행됩니다.

### 개발 모드로 실행

```powershell
npm.cmd install
npm.cmd run desktop:dev
```

PowerShell에서 `npm.ps1` 실행 정책 오류가 나면 `npm` 대신 `npm.cmd`를 사용하세요.

### 실행 파일 다시 만들기

```powershell
npm.cmd run desktop:build
```

빌드 결과는 다음 위치에 생성됩니다.

```text
release\ExamAnalysis-win32-x64\ExamAnalysis.exe
```

빌드 중 `EBUSY` 또는 `resource busy or locked` 오류가 나면 기존에 켜 둔 `ExamAnalysis.exe`를 모두 종료한 뒤 다시 빌드하세요.

## 2. 기본 사용 순서

1. 프로그램을 실행합니다.
2. 직전에 분석한 기록이 있으면 홈 화면에서 바로 `분석 결과 보기`를 눌러 확인할 수 있습니다.
3. 새 분석을 하려면 홈 화면에서 `문항정보표` 엑셀 파일을 선택합니다.
4. 홈 화면에서 `정오표` 엑셀 파일을 선택합니다. 여러 학급을 통합 분석하려면 여러 정오표 파일을 한 번에 선택하거나 함께 드래그해서 올립니다.
5. 파일은 클릭해서 선택하거나, 업로드 박스에 드래그해서 올릴 수 있습니다.
6. `데이터 불러오기`를 누릅니다.
7. 대시보드, 문항분석, 학생목록, 학생별 상세 화면을 확인합니다.

지원 파일 형식:

```text
.xlsx
.xls
```

## 3. 엑셀 파일 구성

프로그램은 기본적으로 두 종류의 엑셀 파일을 사용합니다.

전문 IRT 분석에서 서답형까지 포함하려면 선택 사항으로 `서답형 IRT 입력파일`을 추가로 사용할 수 있습니다.

### 문항정보표

문항 번호, 유형, 내용영역, 성취기준, 난이도, 배점, 정답이 들어 있는 파일입니다.
상단에 있는 과목명, 학년도, 학기, 학년, 고사 차수, 고사일자, 선택형/서답형 총점은 파일 내용에서 자동으로 읽어 분석 화면에 반영됩니다.

주요 열:

```text
문항번호 | 내용영역 | 성취기준 | 난이도 | 배점 | 정답
```

### 정오표

학생별 선택형 응답, 서답형 점수, 총점이 들어 있는 파일입니다. 여러 학급 파일을 동시에 올리면 학생 데이터를 합쳐 전체 학급 통합 분석을 수행합니다.

여러 정오표를 함께 사용할 때는 모든 파일의 선택형 정답키, 배점, 서답형 통합/분리 구조가 같아야 합니다. 같은 학생이 두 파일에 중복으로 들어 있으면 분석이 왜곡될 수 있어 프로그램이 오류로 알려줍니다.

주요 정보:

```text
학번 | 반 | 번호 | 이름 | 선택형 응답 | 서답형 점수 | 총점
```

자세한 샘플 구조는 `data` 폴더의 예시 파일을 참고하세요.

### 서답형 IRT 입력파일 선택 사항

전문 IRT 탭에서 서답형까지 분석하려면 별도의 서답형 IRT 입력파일을 추가로 선택합니다.

이 파일이 없으면 프로그램은 선택형 문항에 대해서만 전문 IRT 분석을 수행합니다.

필수 시트:

```text
문항_루브릭
학생_문항점수
범주_채점기준
```

`문항_루브릭` 시트에는 서답형 문항의 ID, 유형, 내용영역, 성취기준, 최대점, 점수 범주가 들어갑니다.

```text
item_id | item_type | content_area | achievement_standard | max_score | category_values | ordered_categories | model_hint | include_in_irt
```

`학생_문항점수` 시트에는 학생별 서답형 문항 원점수를 입력합니다. 문항 열 이름은 `문항_루브릭`의 `item_id`와 정확히 같아야 합니다.

```text
student_id | class | seat | name | SA01 | SA02 | ES01 | ...
```

중요:

```text
실제 0점은 숫자 0으로 입력
결측 또는 미응시는 빈칸으로 입력
문항별 점수는 0 이상 max_score 이하
점수 범주는 낮은 성취에서 높은 성취로 순서가 있어야 함
```

`범주_채점기준` 시트에는 각 점수 범주가 무엇을 의미하는지 입력합니다.

```text
item_id | category_score | category_label | rubric_description | ordered_step | interpretation_note
```

예시 파일:

```text
C:\tmp\exam_analysis_outputs\irt_subjective_template\subjective_irt_input_example.xlsx
```

## 4. 주요 화면

### 대시보드

전체 평균, 최고점, 최저점, 표준편차, 점수 분포, 내용영역별 정답률, 난이도별 결과를 확인합니다.

### 문항분석

문항별 정답률, 오답 분포, IRT 지표를 확인합니다.

### 전문 IRT

선택형 문항의 정오 반응을 이용해 전문 IRT 분석을 확인합니다.

표시되는 주요 정보:

```text
적용 모형: 1PL Rasch 또는 2PL
학생 능력 추정치 θ
문항 변별도 a
문항 난이도 b
문항 적합도 infit/outfit
검사 정보함수
분리 신뢰도
보정 제외 문항
```

서답형 IRT 입력파일을 함께 불러온 경우에는 같은 화면에 `서답형 다분형 IRT` 섹션이 추가됩니다.

서답형 다분형 IRT에서 표시되는 주요 정보:

```text
서답형 학생 θ
문항별 평균득점률
문항 위치 location
범주 임계값 τ
문항 적합도 infit/outfit
서답형 검사 정보함수
서답형 상위/보완 우선 학생
```

해석할 때는 `데이터 적합성 점검` 또는 `서답형 데이터 점검`에 표시되는 경고를 먼저 확인하세요. 표본 수, 문항 수, 결측 비율, 제외 문항이 많으면 학생별 θ보다 문항별 진단과 루브릭 점검을 우선하는 것이 안전합니다.

### 학생목록

학생별 점수, 석차, 학습유형, 취약 영역을 확인합니다.

사용 가능한 기능:

```text
관찰기록 XLSX 내보내기
개별 분석 일괄 출력
인쇄
```

`관찰기록 XLSX 내보내기`에는 학생별 관찰기록 초안, 환류 문장, 교사 검수 기록, 교사 검수 저장 시각이 포함됩니다.

### 학생 상세

학생별 점수, 문항별 응답, 내용영역별 분석, IRT 기반 분석, 관찰 기록 초안, AI 세특 초안 생성, 교사 검수 기록을 확인합니다.

## 5. 인쇄

각 화면의 `인쇄` 버튼을 누르면 현재 화면을 A4 기준으로 출력합니다.

인쇄 모드에서는 다음을 보정합니다.

```text
화면의 카드 좌우 배치 유지
카드와 표 행이 페이지 중간에서 잘리지 않도록 처리
표 헤더 반복 출력
불필요한 버튼과 메뉴 숨김
```

학생목록의 `개별 분석 일괄 출력`은 별도 인쇄 창을 열어 학생별 분석지를 한 번에 출력합니다.

## 6. AI 세특 초안 생성

학생 상세 화면의 `AI 세특 초안 생성` 영역에서 사용할 수 있습니다.

지원 제공자:

```text
Claude
GPT
Gemini
```

지원 방식:

```text
API 방식
CLI 방식
```

일반 분석 기능은 불러온 엑셀 데이터를 이 프로그램 안에서 처리합니다. 다만 AI 세특 초안 생성을 실행하면 학생 성적, 이름, 문항 반응 등 민감한 데이터가 선택한 외부 AI 서비스 또는 AI CLI로 전송될 수 있습니다. AI가 생성한 결과물은 반드시 교사가 검수하고, 학교 또는 기관의 개인정보 처리 기준을 확인한 뒤 사용하세요.

## 7. API 방식 설정

API 방식은 `.env` 파일의 API 키를 사용합니다.

프로젝트 루트, 즉 `package.json`이 있는 폴더에 `.env` 파일을 만듭니다.

```text
D:\00_gitHub\exam-analysis\.env
```

내용 예시:

```env
VITE_ANTHROPIC_API_KEY=여기에_Claude_API_Key
VITE_OPENAI_API_KEY=여기에_OpenAI_API_Key
VITE_GEMINI_API_KEY=여기에_Gemini_API_Key
```

`.env.example` 파일을 복사해서 `.env`로 이름을 바꾼 뒤 값을 채워도 됩니다.

중요:

```text
API 방식의 VITE_* 값은 빌드 시점에 앱에 포함됩니다.
.env 파일을 수정한 뒤에는 npm.cmd run desktop:build 로 다시 빌드해야 실행 파일에 반영됩니다.
API 키가 포함된 실행 파일을 다른 사람에게 배포하지 마세요.
여러 컴퓨터에서 안전하게 쓰려면 API 방식보다 CLI 방식이 더 적합합니다.
```

### Claude API 키

1. Anthropic Console에 접속합니다.
2. API Keys에서 새 키를 생성합니다.
3. `.env`에 입력합니다.

```env
VITE_ANTHROPIC_API_KEY=sk-ant-...
```

공식 문서:

```text
https://platform.claude.com/docs/en/api/authentication/overview
```

### OpenAI API 키

1. OpenAI API key 페이지에 접속합니다.
2. 새 Secret key를 생성합니다.
3. `.env`에 입력합니다.

```env
VITE_OPENAI_API_KEY=sk-...
```

공식 문서:

```text
https://platform.openai.com/api-keys
https://platform.openai.com/docs/api-reference/authentication
```

### Gemini API 키

1. Google AI Studio에 접속합니다.
2. Gemini API key를 생성합니다.
3. `.env`에 입력합니다.

```env
VITE_GEMINI_API_KEY=...
```

공식 문서:

```text
https://aistudio.google.com/apikey
https://ai.google.dev/aistudio
```

## 8. CLI 방식 설정

CLI 방식은 각 컴퓨터에 설치된 외부 명령어를 실행합니다.

장점:

```text
API 키를 실행 파일에 넣지 않아도 됨
컴퓨터별 로그인 상태를 사용할 수 있음
배포용으로 상대적으로 관리하기 쉬움
```

주의:

```text
새 컴퓨터에서는 CLI를 다시 설치하고 로그인해야 합니다.
설치 후 PowerShell과 프로그램을 완전히 종료했다가 다시 실행해야 PATH가 반영됩니다.
```

### Node.js 설치

CLI 설치 전에 Node.js가 필요합니다.

확인:

```powershell
node -v
npm.cmd -v
```

없으면 설치:

```powershell
winget install OpenJS.NodeJS.LTS
```

설치 후 PowerShell을 닫고 새로 여세요.

### Claude CLI 설치

방법 1: WinGet

```powershell
winget install Anthropic.ClaudeCode
```

방법 2: npm

```powershell
npm.cmd install -g @anthropic-ai/claude-code
```

로그인 또는 초기 실행:

```powershell
claude
```

또는:

```powershell
claude auth login
```

확인:

```powershell
where.exe claude
claude --version
```

프로그램 기본 CLI 템플릿:

```text
claude --print --model {model} {prompt}
```

공식 문서:

```text
https://code.claude.com/docs/en/installation
https://code.claude.com/docs/en/cli-usage
```

### Codex CLI 설치

설치:

```powershell
npm.cmd install -g @openai/codex
```

로그인:

```powershell
codex --login
```

확인:

```powershell
where.exe codex
codex --version
```

프로그램 기본 CLI 템플릿:

```text
codex exec --model {model} {prompt}
```

프로그램 내부에서는 Codex 프롬프트를 stdin으로 전달합니다.

공식 문서:

```text
https://help.openai.com/en/articles/11381614-api-codex-cli-and-sign-in-with-chatgpt
https://help.openai.com/en/articles/11096431-openai-codex-cli-getting-started
```

### Gemini CLI 설치

설치:

```powershell
npm.cmd install -g @google/gemini-cli
```

로그인:

```powershell
gemini
```

인증 방식 선택 화면이 나오면 보통 다음을 선택합니다.

```text
1. Login with Google
```

폴더 신뢰 질문이 나오면 현재 프로젝트 폴더만 신뢰하세요.

```text
1. Trust folder (exam-analysis)
```

`Trust parent folder (Users)`는 너무 넓으므로 권장하지 않습니다.

확인:

```powershell
where.exe gemini
gemini --version
```

테스트:

```powershell
"안녕. 한 문장으로 답해줘." | gemini --model flash --skip-trust --prompt "Respond to the prompt provided on stdin."
```

프로그램 기본 CLI 템플릿:

```text
gemini --model {model} --prompt {prompt}
```

프로그램 내부에서는 Gemini 프롬프트를 stdin으로 전달하고 `--skip-trust`를 자동으로 보강합니다.

공식 문서:

```text
https://google-gemini.github.io/gemini-cli/docs/get-started/
https://google-gemini.github.io/gemini-cli/docs/get-started/authentication.html
```

### Gemini CLI 인증 오류 해결

다음 오류가 나오면 Gemini CLI는 설치됐지만 인증이 안 된 상태입니다.

```text
Please set an Auth method in C:\Users\...\ .gemini\settings.json
or specify GEMINI_API_KEY, GOOGLE_GENAI_USE_VERTEXAI, GOOGLE_GENAI_USE_GCA
```

해결 방법 1: Google 로그인

```powershell
gemini
```

인증 방식에서 `Login with Google`을 선택합니다.

해결 방법 2: Gemini API 키를 환경 변수로 등록

```powershell
setx GEMINI_API_KEY "여기에_Gemini_API_Key"
```

등록 후 PowerShell과 프로그램을 완전히 종료했다가 다시 실행하세요.

## 9. CLI 템플릿 직접 수정

AI 세특 초안 생성 영역에서 `CLI` 모드를 선택하면 CLI 명령어 템플릿을 수정할 수 있습니다.

템플릿에는 다음 자리표시자를 사용할 수 있습니다.

```text
{model}
{prompt}
```

예를 들어 CLI가 PATH에 잡히지 않는 컴퓨터에서는 전체 경로를 넣을 수 있습니다.

```text
"C:\Users\사용자\AppData\Roaming\npm\claude.cmd" --print --model {model} {prompt}
```

```text
"C:\Users\사용자\AppData\Roaming\npm\codex.cmd" exec --model {model} {prompt}
```

```text
"C:\Users\사용자\AppData\Roaming\npm\gemini.cmd" --model {model} --prompt {prompt}
```

CLI 템플릿은 현재 컴퓨터의 브라우저 저장소에 저장됩니다. 다른 컴퓨터로 옮기면 다시 설정해야 할 수 있습니다.

## 10. 자주 나는 오류

### spawn claude ENOENT

뜻:

```text
claude 명령어를 찾을 수 없음
```

해결:

```powershell
npm.cmd install -g @anthropic-ai/claude-code
where.exe claude
```

설치 후 프로그램을 다시 실행하세요.

### spawn codex ENOENT

뜻:

```text
codex 명령어를 찾을 수 없음
```

해결:

```powershell
npm.cmd install -g @openai/codex
codex --login
where.exe codex
```

### spawn gemini ENOENT

뜻:

```text
gemini 명령어를 찾을 수 없음
```

해결:

```powershell
npm.cmd install -g @google/gemini-cli
gemini
where.exe gemini
```

### API 키가 설정되지 않았다는 오류

확인할 것:

```text
.env 파일 이름이 정확한지 확인
VITE_ 접두사가 붙어 있는지 확인
키 앞뒤에 따옴표나 공백이 잘못 들어가지 않았는지 확인
.env 수정 후 다시 빌드했는지 확인
```

다시 빌드:

```powershell
npm.cmd run desktop:build
```

### PowerShell에서 npm 실행이 막힘

오류 예:

```text
이 시스템에서 스크립트를 실행할 수 없으므로 npm.ps1 파일을 로드할 수 없습니다.
```

해결:

```powershell
npm.cmd install
npm.cmd run build
npm.cmd run desktop:build
```

### CLI 설치 후에도 프로그램에서 못 찾음

확인:

```powershell
where.exe claude
where.exe codex
where.exe gemini
```

해결:

```text
PowerShell 새로 열기
성적분석 프로그램 완전히 종료 후 재실행
그래도 안 되면 CLI 템플릿에 전체 경로 입력
```

## 11. 보안 주의사항

1. API 키를 다른 사람에게 공유하지 마세요.
2. API 키가 들어간 `.env` 파일을 GitHub에 올리지 마세요.
3. API 방식으로 빌드한 실행 파일에는 키가 포함될 수 있으므로 배포에 주의하세요.
4. 일반 분석만 사용할 때는 AI 서비스로 데이터를 보내지 않습니다. AI 세특 초안 생성을 실행할 때만 학생 이름, 성적, 응답 데이터가 선택한 AI 서비스 또는 AI CLI로 전송될 수 있습니다.
5. AI 결과는 초안일 뿐이며, 최종 기록은 교사가 검수해야 합니다.
6. 공동 사용 컴퓨터에서는 CLI 로그인 계정과 API 키를 반드시 확인하세요.

## 12. 새 컴퓨터 설치 체크리스트

새 컴퓨터에서 AI CLI 기능까지 사용하려면 아래를 확인하세요.

```text
Node.js 설치
Claude CLI 설치 및 로그인
Codex CLI 설치 및 로그인
Gemini CLI 설치 및 로그인
성적분석 프로그램 재실행
where.exe 명령으로 CLI 경로 확인
```

빠른 설치 명령:

```powershell
winget install OpenJS.NodeJS.LTS
npm.cmd install -g @anthropic-ai/claude-code
npm.cmd install -g @openai/codex
npm.cmd install -g @google/gemini-cli
```

로그인:

```powershell
claude
codex --login
gemini
```

설치 확인:

```powershell
where.exe claude
where.exe codex
where.exe gemini
```

## 13. 참고 링크

```text
Claude Code 설치:
https://code.claude.com/docs/en/installation

Claude API 인증:
https://platform.claude.com/docs/en/api/authentication/overview

OpenAI API 키:
https://platform.openai.com/api-keys

OpenAI API 인증:
https://platform.openai.com/docs/api-reference/authentication

Codex CLI:
https://help.openai.com/en/articles/11381614-api-codex-cli-and-sign-in-with-chatgpt

Gemini CLI:
https://google-gemini.github.io/gemini-cli/docs/get-started/

Gemini CLI 인증:
https://google-gemini.github.io/gemini-cli/docs/get-started/authentication.html

Gemini API 키:
https://aistudio.google.com/apikey
```
